# Dark mode menu fix

Dark mode is now directly visible in the compact top toolbar as a Dark/Light button and also available in All functions → Actions → Dark / light mode.

The setting is saved in localStorage under `pcb-forge-theme` and applied as `body.dark`.
