# UI all functions dropdown

The top bar now has:

- Workspace dropdown: switches the left tool panel.
- All functions dropdown: every action and workspace is reachable without adding more horizontal buttons.
- Essential tools: Select, Route, Text, DRC, Left, Right.

This avoids menu overlap while preserving access to all features.
